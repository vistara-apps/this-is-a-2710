// Privy App ID
export const PRIVY_APP_ID = import.meta.env.VITE_PRIVY_APP_ID || 'clxyz-placeholder-id';

// Airstack API Key
export const AIRSTACK_API_KEY = import.meta.env.VITE_AIRSTACK_API_KEY || 'placeholder-api-key';

// Base Explorer URL
export const BASE_EXPLORER_URL = 'https://basescan.org';

// API Endpoints
export const API_ENDPOINTS = {
  // Privy endpoints
  PRIVY_AUTH: 'https://auth.privy.io/api/v1',
  
  // Airstack endpoints
  AIRSTACK_API: 'https://api.airstack.xyz/gql',
  
  // Base RPC endpoint
  BASE_RPC: 'https://mainnet.base.org',
};

